<div class="container">
  <div class="d-flex coffee-cards-container" id="cards-container">
    <% @coffees.each do |coffee| %>
      <%= render "coffees/coffee_card", coffee: coffee%>
    <% end %>
    </div>
</div>
